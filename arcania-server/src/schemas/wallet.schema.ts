import { z } from 'zod';

// wallet/spin takes no body — no schema needed
// wallet/use-token takes no body — no schema needed
// This file is a placeholder for future wallet request schemas.
export const _placeholder = z.object({});
